{
  "name": "ghostkeys",
  "private": true,
  "description": "GhostKeys monorepo — deployable web app lives in web/",
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "start": "npm run start -w web",
    "preview": "npm run preview -w web"
  }
}
